namespace GT2.SaveEditor.Settings.Controller
{
    public enum ControllerButtonEnum
    {
        Up,
        Down,
        Left,
        Right,
        L1,
        L2,
        L3, // Doesn't exist on a digital controller, not properly supported, gives broken graphics on the settings screen
        Triangle = 8,
        X,
        Square,
        Circle,
        R1,
        R2,
        R3 // Doesn't exist on a digital controller, not properly supported, gives broken graphics on the settings screen
    }
}